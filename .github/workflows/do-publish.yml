name: Publish

on:
  push:
    branches: # Snapshots
      - main
    tags: ["*"] # Releases
  create:
    tags:
      - "**"

jobs:
  publish-artifacts:
    name: Publish / Artifacts
    run: |
      echo ${{ github.event_name }}
      echo ${{ github.event.ref_type }}
      echo ${{ github.ref }}
